@model WordViewModel

<form method="post">
    <div class="form-group">
        <label asp-for="@Model.Word.Spanish">@Model.Word.Spanish</label>
        <input asp-for="@Model.Word.Spanish">
    </div>
    <div class="form-group">
        <label asp-for="@Model.Word.English">@Model.Word.English</label>
        <input asp-for="@Model.Word.English">
    </div>
    <button type="submit" class="btn btn-primary">Create</button>
</form>